(ns simpleblog.core
  (:require [org.httpkit.server :as s]
            [compojure.core :refer :all])
  )

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Let's create a blog page!"))
